<?php require '../config.php'; if(admin_logged()){header('Location:index.php');exit;} $error=''; if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$email=trim($_POST['email']??'');$pass=$_POST['password']??'';$st=db()->prepare("SELECT * FROM admins WHERE email=?");$st->bind_param('s',$email);$st->execute();$a=stmt_one($st);if($a&&password_verify($pass,$a['password_hash'])){$_SESSION['admin_id']=$a['id'];header('Location:index.php');exit;} $error='Invalid credentials.';} ?>
<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin · HireFlow</title><link rel="stylesheet" href="../assets/style.css"></head><body class="admin-login"><div class="login-card"><a class="brand" href="../index.php"><span class="brand-mark">H</span><span>HireFlow</span></a><span class="eyebrow">ADMIN CONSOLE</span><h1>Recruiting workspace</h1><?php if($error):?><div class="error"><?=e($error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=csrf()?>"><label>Email<input type="email" name="email" value="<?=e(ADMIN_EMAIL)?>" required></label><label>Password<input type="password" name="password" value="<?=e(ADMIN_PASSWORD)?>" required></label><button class="btn dark full">Sign in</button></form><p class="hint">Default: <?=e(ADMIN_EMAIL)?> / <?=e(ADMIN_PASSWORD)?></p></div></body></html>